// import React, { Component } from "react";
// import "./style.css";

function Button(props) {
    return (
     <button
     onClick={props.sort}>
         Sort by name
     </button>
    );
  }

export default Button;
